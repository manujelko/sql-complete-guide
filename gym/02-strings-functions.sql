-- SELECT CONCAT(first_name, ' ', last_name)
-- FROM memberships;

-- postgreSQL only
-- SELECT first_name || ' ' || last_name
-- FROM memberships;

-- SELECT CONCAT('$ ', price)
-- FROM memberships;

-- SELECT LOWER('ABC');

-- SELECT LENGTH('ABC');

-- LEADING
-- TRAILING
-- BOTH
SELECT TRIM(TRAILING ' ' FROM 'male   ');